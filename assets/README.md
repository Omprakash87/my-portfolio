# Assets folder for portfolio files

- **hero-bg.mp4** — Optional. Add a video file named `hero-bg.mp4` here to show a subtle background video across the whole site. It plays full-page (muted, looped). If the file is missing, the circuit images are used as background.
